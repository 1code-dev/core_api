/**
 * @type Global type for exercise w/ details
 */
export interface TExerciseDetails {
  id: string;
  name: string;
  maxPoints: number;
  minPoints: number;
  instructions: string;
  baseCode: string;
}
