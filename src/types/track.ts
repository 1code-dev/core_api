/**
 * @type Global type for available Tracks on 1Code
 */
export interface TAvailableTrack {
  // uid of track
  id: string;

  // name of the track
  name: string;

  // tags of track
  tags: string[];

  // logo of the track
  logo: string;
}
